import type { ConfigButterCMS } from "../commerce/butterCMS/client.ts";

/**
 * @title Butter CMS Account
 */
export default function ButterCMS(props: ConfigButterCMS): ConfigButterCMS {
  return props;
}
